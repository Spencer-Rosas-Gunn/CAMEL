# CAMEL
To use the library, `#include "camel.hpp"` and `#include "window.hpp"`. This is an entirely header based library, and it's based on the Windows API (so it *only* works on Windows). For examples of usage, see `main.cpp`. 
